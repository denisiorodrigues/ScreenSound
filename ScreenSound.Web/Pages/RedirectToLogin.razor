@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/login?ReturnUrl={Uri.EscapeUriString(NavigationManager.Uri)}");
    }
}
