info:
  name: artistas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/artistas"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
